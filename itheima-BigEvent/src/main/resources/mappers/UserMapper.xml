<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lordbao.bigevent.mapper.UserMapper">
    <insert id="add">
        INSERT INTO user(username, password, nickname,email,create_time, update_time)
        VALUES (#{username},#{password},#{nickname},#{email},now(),now())
    </insert>

    <update id="update">
        UPDATE user SET
        nickname=#{nickname},email =#{email}, update_time=now()
        WHERE id=#{id}
    </update>
    <update id="updateAvatar">
        UPDATE user SET
        user_pic =#{avatarUrl},update_time=now()
        WHERE id =#{id}
    </update>
    <update id="updatePWd">
        UPDATE  user SET
        password=#{newPwd},update_time=now()
        WHERE id=#{id}
    </update>


    <select id="findByUsername" resultType="com.lordbao.bigevent.pojo.User">
        SELECT  * FROM  user
        WHERE  username = #{username}
    </select>

    <select id="findByUsernameAndPassword" resultType="com.lordbao.bigevent.pojo.User">
        SELECT * FROM user
        WHERE username=#{username} AND password =#{password}
    </select>
</mapper>