<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lordbao.bigevent.mapper.ArticleMapper">

    <insert id="addArticle">
        insert into article(title, content, cover_img, state,category_id, create_user, create_time, update_time)
        values (#{title},#{content},#{coverImg},#{state},#{categoryId},#{createUser},now(),now())
    </insert>
    <select id="list" resultType="com.lordbao.bigevent.pojo.Article">
        select id, title, content, cover_img, state, category_id, create_time, update_time
        from article

        <where>
            create_user=#{userId}
            <if test="categoryId!=null">
                and category_id = #{categoryId}

           </if>
            <if test="state!=null">
                and state = #{state}
            </if>

        </where>
    </select>



</mapper>